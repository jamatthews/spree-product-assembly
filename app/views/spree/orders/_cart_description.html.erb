<% product = variant.product %>
<% if product.assembly? %>
    <% line_item.part_line_items.each do |pli| %>
      <% pli_variant = pli.variant %>
      <% unless pli_variant.is_master? %>
        <br><%= pli_variant.options_text %>
      <% end %>
    <% end %>
<% end %>
